<UserControl x:Class="DotEngineEditor.UserControls.HierarchyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/DotEngineEditor.Themes;component/HierarchyPanelTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource PanelBackgroundBrush}"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <Border Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="5"
                Margin="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="{DynamicResource HeaderBackgroundBrush}">
                    <TextBlock Text="Hierarchy"
                               Foreground="{DynamicResource HeaderForegroundBrush}"
                               Padding="5"
                               VerticalAlignment="Center"/>
                </Border>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Background="{DynamicResource TreeBackgroundBrush}">
                    <TreeView Background="{DynamicResource TreeBackgroundBrush}"
                              Foreground="{DynamicResource TreeForegroundBrush}"
                              BorderThickness="0"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch">
                        <TreeViewItem Header="Scene" IsExpanded="True">
                            <TreeViewItem Header="Main Camera"/>
                            <TreeViewItem Header="Directional Light"/>
                            <TreeViewItem Header="Game Objects" IsExpanded="True">
                                <TreeViewItem Header="Cube"/>
                                <TreeViewItem Header="Sphere"/>
                            </TreeViewItem>
                        </TreeViewItem>
                    </TreeView>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
